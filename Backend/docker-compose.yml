services:
  mongodb:
    image: mongo:7
    container_name: cinepass-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: cinepass
    volumes:
      - cinepass-mongodb-data:/data/db

volumes:
  cinepass-mongodb-data:
